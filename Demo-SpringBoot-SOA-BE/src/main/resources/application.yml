server:
  port: 8081  # mặc định cho user-service

spring:
  application:
    name: demo-soa
  datasource:
    url: jdbc:mysql://localhost:3307/user_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 040904
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

services:
  user:
    port: 8081
    db: user_db
  room:
    port: 8082
    db: room_db
  review:
    port: 8083
    db: review_db
